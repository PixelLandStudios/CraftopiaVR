using UnityEngine;

public class WoodPlank : MonoBehaviour
{
    public GameObject ParentPlank;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
